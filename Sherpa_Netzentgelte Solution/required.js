function my_login(userName, password) {    if ((userName == 'Designer') || (userName =='')) {        return false;    }    else {        var my_User = ds.Benutzer.query('name == :1', userName).first();        if (my_User != undefined) {            if (my_User.password == password) {                return {                    ID: my_User.userid,                    name: my_User.name,                    fullName: my_User.fullName,                    belongsTo: ['User'],                    storage: {} // a Storage object which is the sessionStorage property of the user session                }; // Ende return object            }            else {                return {                    error: 10,  errorMessage:'Passwort falsch!'                };            };        }        else {            return {                error: 10, errorMessage:'Benutzername falsch!'            };        }    };};